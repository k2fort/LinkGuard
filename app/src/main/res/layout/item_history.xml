<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="12dp"
    android:layout_marginVertical="4dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="2dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- ── Summary row (always visible, tappable) ── -->
        <LinearLayout
            android:id="@+id/layoutHeader"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="top"
            android:paddingHorizontal="12dp"
            android:paddingVertical="12dp"
            android:foreground="?attr/selectableItemBackground"
            android:clickable="true"
            android:focusable="true">

            <!-- Verdict icon -->
            <ImageView
                android:id="@+id/ivHistoryIcon"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:layout_marginEnd="10dp"
                android:layout_marginTop="2dp"
                android:contentDescription="@string/overlay_verdict_icon_desc" />

            <!-- Centre column: verdict label + URL + first reason -->
            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical">

                <!-- Verdict level label (e.g. "קישור מסוכן" / "בטוח") -->
                <TextView
                    android:id="@+id/tvVerdictLabel"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="11sp"
                    android:textStyle="bold"
                    android:letterSpacing="0.04"
                    android:layout_marginBottom="2dp" />

                <!-- The URL itself — up to 2 lines -->
                <TextView
                    android:id="@+id/tvHistoryUrl"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:ellipsize="middle"
                    android:maxLines="2"
                    android:textSize="12sp"
                    android:textColor="?attr/colorOnSurface"
                    android:fontFamily="monospace" />

                <!-- First reason / summary -->
                <TextView
                    android:id="@+id/tvHistoryReason"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:ellipsize="end"
                    android:maxLines="1"
                    android:textSize="11sp"
                    android:textColor="?attr/colorOnSurfaceVariant"
                    android:layout_marginTop="3dp" />

            </LinearLayout>

            <!-- Right column: timestamp + scan count -->
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:gravity="end"
                android:layout_marginStart="8dp">

                <TextView
                    android:id="@+id/tvHistoryTime"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="11sp"
                    android:textColor="?attr/colorOnSurfaceVariant" />

                <TextView
                    android:id="@+id/tvHistoryDate"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textSize="11sp"
                    android:textColor="?attr/colorOnSurfaceVariant"
                    android:layout_marginTop="2dp" />

                <!-- Scan count badge — hidden when count = 1 -->
                <TextView
                    android:id="@+id/tvScanCount"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="4dp"
                    android:paddingHorizontal="6dp"
                    android:paddingVertical="1dp"
                    android:textSize="10sp"
                    android:textColor="#FFFFFF"
                    android:background="@drawable/bg_scan_count"
                    android:visibility="gone" />

            </LinearLayout>

            <!-- Expand chevron -->
            <TextView
                android:id="@+id/tvChevron"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="6dp"
                android:layout_marginTop="4dp"
                android:text="▾"
                android:textSize="14sp"
                android:textColor="#AAAAAA" />

        </LinearLayout>

        <!-- ── Divider (shown when expanded) ── -->
        <View
            android:id="@+id/dividerDetail"
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="?attr/colorOutline"
            android:visibility="gone" />

        <!-- ── Expanded detail section ── -->
        <LinearLayout
            android:id="@+id/layoutDetail"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingHorizontal="16dp"
            android:paddingTop="10dp"
            android:paddingBottom="12dp"
            android:visibility="gone">

            <!-- Source app -->
            <TextView
                android:id="@+id/tvDetailSource"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="12sp"
                android:textColor="?attr/colorOnSurfaceVariant"
                android:layout_marginBottom="6dp" />

            <!-- Original (shortened) URL — only shown when it differs from resolved -->
            <TextView
                android:id="@+id/tvDetailOriginalUrl"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="12sp"
                android:textColor="?attr/colorOnSurfaceVariant"
                android:layout_marginBottom="6dp"
                android:visibility="gone" />

            <!-- All reasons as bullet list -->
            <TextView
                android:id="@+id/tvDetailReasons"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="12sp"
                android:textColor="?attr/colorOnSurface"
                android:lineSpacingExtra="3dp"
                android:visibility="gone" />

            <!-- Trust domain button — lets user whitelist directly from scan log -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnTrustDomain"
                style="@style/Widget.Material3.Button.TextButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:visibility="gone" />

        </LinearLayout>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
