<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission"
        xmlns:tools="http://schemas.android.com/tools" />

    <application
        android:name=".LinkGuardApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LinkGuard"
        android:localeConfig="@xml/locales_config">

        <!-- Main entry point -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Onboarding flow (shown once on first launch) -->
        <activity
            android:name=".ui.OnboardingActivity"
            android:exported="false" />

        <!-- App selector screen -->
        <activity
            android:name=".ui.AppSelectorActivity"
            android:exported="false"
            android:label="@string/title_app_selector" />

        <!-- Scan history log -->
        <activity
            android:name=".ui.HistoryActivity"
            android:exported="false"
            android:label="@string/title_history" />

        <!-- Settings screen -->
        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false"
            android:label="@string/title_settings" />

        <!-- Whitelist manager -->
        <activity
            android:name=".ui.WhitelistActivity"
            android:exported="false"
            android:label="@string/title_whitelist" />

        <!-- Notification Listener Service — core of the app -->
        <service
            android:name=".service.LinkGuardNotificationService"
            android:exported="true"
            android:label="@string/service_label"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
            <!-- No default_filter_types filter — we must receive ALL notification types,
                 including "silent" ones. Users commonly mute messaging apps (WhatsApp
                 group chats, etc.), which downgrades notifications to the "silent" category
                 on Android 11+. Filtering to "conversations|alerting" only would silently
                 drop phishing links arriving in muted chats. -->
        </service>

        <!-- Foreground service — keeps the process alive when app is closed -->
        <service
            android:name=".service.KeepAliveService"
            android:foregroundServiceType="specialUse"
            android:exported="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="notification-url-scanner" />
        </service>

        <!-- Accessibility service — scans links when user opens a monitored app -->
        <service
            android:name=".service.LinkGuardAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true"
            android:label="@string/accessibility_service_label">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Boot receiver — restart service after reboot -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
